import pandas as pd


def hitrate_score(y_true: pd.Series, y_pred: pd.Series, at_k: int = 3):
    return 0


def mean_average_precision_score(y_true: pd.Series, y_pred: pd.Series, at_k: int = 3):
    return 0


def ndcg_score(y_true: pd.Series, y_pred: pd.Series, at_k: int = 3):
    return 0

